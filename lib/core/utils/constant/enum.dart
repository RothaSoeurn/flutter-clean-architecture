enum LoggerType {
  debug,
  info,
  error,
}

enum ApiStatus {
  success,
  failed,
}

enum MessageType {
  success,
  warning,
  error,
}

enum LanguageType { en, km }

enum ChatMessageType {
  text,
  image,
  file,
  voice,
  location,
}
